# （ひな形）GUIドキュメント

このファイルは、GUIごとの説明を追加するときのテンプレートです。新規ページを作って、この構成に沿って埋めてください。

## 目的
- 何をしたいときに使うか
- どんな入力を想定しているか

## 入力
- 入力パス:
  - プロジェクトフォルダ（推奨）
  - `.ipro`（プロジェクトファイル）
  - `Result_*.csv` が入ったCSVフォルダ（対応している場合）
- 前提:
  - 例: `I, J, X, Y` が必須 / FlowSolution 必須 など

## 出力
- 出力フォルダ:
- 出力ファイル:
- 既存ファイルがある場合の挙動:

## 画面の使い方
1. 入力を選択
2. 出力先を選択
3. 必要なオプションを設定
4. 実行

## よくあるエラー
- 入力の種類が違う
- 必要な変数（列）が存在しない
- 出力先に書き込めない

## 参考
- マニュアル（ヘルプメニューのリンク）
- 仕様（`docs/specs/*.md`）

